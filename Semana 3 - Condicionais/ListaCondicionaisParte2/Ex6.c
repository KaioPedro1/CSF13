#include <stdio.h>
#define base 432.000
#define altura 468.000
int main()
{
  float x,y;
  printf("Digite as cordenadas do ponto de contato: ");
  scanf("%f" "%f", &x,&y);
  if(x<=base&&y<=altura)
    printf("d");
  else
    printf("f");
  return 0;
}
